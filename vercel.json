{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install --production=false",
  "regions": ["sin1"],
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}